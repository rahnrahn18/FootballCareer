<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. Using standard Android theme as we are using Compose -->
    <style name="Base.Theme.Championstar" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Championstar" parent="Base.Theme.Championstar" />

    <!-- Tema khusus untuk semua activity game -->
    <style name="Theme.Championstar.Game" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    
    <!-- Removed DashboardButton and RoundedImageView styles as they are not used in Compose and were causing resource linking errors -->
</resources>